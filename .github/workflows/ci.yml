name: CI_CD
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Secret auswerten
        run: >
          echo "Länge des Secrets: " 
          length(${{ secrets.DEMO_SECRET }})
